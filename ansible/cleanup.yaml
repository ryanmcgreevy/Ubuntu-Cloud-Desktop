---
- name: Cleanup instance to prep for marketplace ami creation
  hosts: tag_Name_fresh_install
  become: true
  gather_facts: false

  tasks:
    #- name: shred keys
    #  ansible.builtin.command: "shred -u /etc/ssh/*_key /etc/ssh/*_key.pub"

    - name: remove ubuntu authorized keys
      file:
        path: /home/ubuntu/.ssh/authorized_keys
        state: absent

    - name: remove root authorized keys
      file:
        path: /root/.ssh/authorized_keys
        state: absent

    - name: remove ssh public key
      file:
        path: /home/ubuntu/.ssh/id_rsa.pub
        state: absent 

    - name: remove bash history
      file:
        path: /home/ubuntu/.bash_history
        state: absent 

    - name: remove known hosts
      file:
        path: /home/ubuntu/.ssh/known_hosts
        state: absent 

    - name: remove crash logs
      file:
        path: "/var/crash/*"
        state: absent 

    - name: clear terminal history
      shell: history -cw
      args:
        executable: /bin/bash

    # - name: wait for cloud-init to finish
    #   ansible.builtin.command: cloud-init status --wait
    #   changed_when: false

    # - name: Clean cloud-init state for next boot
    #   ansible.builtin.command: cloud-init clean --logs --seed

    # - name: Reset machine id for image clone
    #   ansible.builtin.command: truncate -s 0 /etc/machine-id

    # - name: Remove dbus machine id if present
    #   ansible.builtin.file:
    #     path: /var/lib/dbus/machine-id
    #     state: absent
